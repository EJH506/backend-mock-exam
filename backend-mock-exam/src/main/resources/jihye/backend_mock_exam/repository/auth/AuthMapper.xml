<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jihye.backend_mock_exam.repository.auth.AuthMapper">

    <insert id="userSave" useGeneratedKeys="true" keyProperty="userId">
        insert into users (account_id, nickname, hashed_password, find_password_question, find_password_answer, gender, birth_year)
        values (#{accountId}, #{nickname}, #{hashedPassword}, #{findPasswordQuestion}, #{findPasswordAnswer}, #{gender}, #{birthYear})
    </insert>

    <select id="isIdExists" resultType="boolean">
        select count(*) > 0
        from users
        where account_id = #{accountId}
    </select>

    <select id="findByLoginId" resultType="user">
        SELECT * FROM users
        WHERE account_id = #{accountId};
    </select>

</mapper>